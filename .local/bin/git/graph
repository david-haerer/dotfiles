#!/bin/sh

git log --graph \
    --abbrev-commit \
    --decorate \
    --format=format:'%C(yellow)%>|(16)%h%C(reset) %C(blue)%ad%C(reset) %C(white)%s%C(reset) %C(dim white)~ %an%C(reset)%C(auto)%d%C(reset)' \
    --date=format:'%Y-%m-%d %H:%M' \
    --all

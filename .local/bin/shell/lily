#!/bin/sh

filepath="$1"
basepath="$(dirname ${filepath})"
filename="$(basename ${filepath})"
name="${filename%.*}"

lilypond $filepath \
    && timidity "$basepath/$name.midi" -Ow -o - \
    | ffmpeg -i - -acodec libmp3lame -ab 64k "$basepath/$name.mp3"
